# NotiMess — Project Memory

Living context doc for this project. Anyone (human or Claude, in a future session) should be able to read this and get oriented without re-deriving decisions from scratch. Update this whenever a real decision gets made or the project status changes — don't let it go stale.

## Snapshot (as of 2026-09-14)

- Project is in **Phase 0 (docs & data modeling)** — see `PLAN.md` for the full phase breakdown. No code written yet, just documentation and one extracted data file.
- The only input asset so far is `menu.jpeg`: a photo of the VIT Bhopal Boys & Girls Hostels mess menu poster, September 2026, "Week 1", effective from 14 Sept 2026.
- That photo has been transcribed into `data/menu.json`. Breakfast and High Tea are high-confidence transcriptions; some Lunch/Dinner cells (esp. Saturday/Sunday) are medium-confidence — see `_extraction_meta` in that file. **Not yet spot-checked against the physical poster.**

## Key decisions & why

- **Name: NotiMess.** User's original name, kept as-is. Alternatives considered (MessPing, PlatePing) were offered but declined.
- **Notification delivery: ntfy.sh**, not Telegram bot or Pushover.
  - *Why:* zero account setup on either end (no bot creation, no paid app), fastest path to a working notification. User picked this over the alternatives when asked directly.
  - *Trade-off accepted:* ntfy.sh's free public server has no built-in access control beyond the topic name being unguessable — acceptable for a menu-content notification (nothing sensitive).
- **Scheduler: GitHub Actions cron**, not a local always-on device.
  - *Why:* doesn't depend on the user's own machine being powered on; free; reliable enough for 4 runs/day. User picked this over local cron when asked directly.
- **No LLM call in the runtime path.** Menu parsing happens once (or whenever the poster changes), interactively with Claude, producing a static JSON file. The 4x/day scheduled job is a plain script with no API dependency.
  - *Why:* cheaper, faster, and removes a whole class of runtime failure (API errors, cost, latency) from something that just needs to read a JSON file and send an HTTP POST.
- **Data model: one repeating 7-day cycle**, not a dated calendar or multi-week rotation.
  - *Why:* that's what the poster actually shows (Mon–Sun, no dates). The poster says "Week 1" which *might* imply a rotation, but we have no Week 2 data to confirm — treated as an open question, not designed around speculatively. See `PRD.md` §11.

## Open questions (unresolved, revisit when info arrives)

- Does the mess actually rotate through multiple weeks, or was "Week 1" a one-off label? Need to see if a "Week 2" poster ever shows up.
- How often does the menu actually change (monthly, per-semester)? Determines how often the manual re-ingestion workflow gets exercised in practice.

## Known constraints / caveats

- The active menu file (`data/menus/vit_bhopal_mess_menu_september_2026.json`) is now the **Week 2** data (since 2026-09-21), supplied by the user directly as structured JSON rather than extracted from a photo. Hasn't been spot-checked against the physical poster yet — still an open action item, same as Week 1 was before the user's own correction.
- A menu file's raw shape can't be assumed to match the canonical schema — it's changed between updates so far (see the 2026-09-21 change log entry). Always verify/convert before use.
- `strip_nonveg()`'s assumptions can't be assumed stable across menu updates either — Week 2 already broke one (see 2026-09-21 entry). Re-check against any new file's `(Non-Veg)`-tagged items before shipping.
- This is a multi-user tool (Telegram bot, self-serve subscribe) — the "single-user personal tool" framing from `PRD.md`'s original V1 scope no longer applies; see Part 2 of `PRD.md` for the current scope.

## V2 decisions & why (multi-user platform)

- **Scope change:** project is expanding from "notify just me" to "let other people subscribe too" via a website. See `PRD.md` Part 2 and `ARCHITECTURE.md` V2 section for full detail.
- **No login required to subscribe.** Recommended and adopted: the public subscribe flow has no username/password/account system. Reasoning: there's no private per-user data to protect (menu content is public), login adds signup friction and a security surface for zero benefit at this stage, and the delivery channel itself already provides a lightweight identity (a Telegram `chat_id`) sufficient to deliver and unsubscribe. Revisit only if/when per-user preferences (hostel choice, meal opt-outs) need durable storage — and even then, the plan is a passwordless mechanism (magic link/OTP), not full auth.
- **Delivery channel for V2: Telegram bot** (recommended over a shared ntfy topic or a Web Push/PWA site). Reasoning: no app-store presence needed, students already have Telegram, free Bot API, chat_id doubles as identity so no separate auth system, backend stays small (one serverless function to capture `/start` + a small store of chat_ids). Shared-ntfy-topic was ruled out for multi-user because a public topic has no spoofing protection (anyone who learns the topic name can publish fake notices to every subscriber). Web Push/PWA was deferred as a future upgrade — more moving parts (VAPID keys, service worker, iOS 16.4+-only support) for marginal gain over Telegram at this stage.
- **Website's job in V2 is just a landing page** (static, no login) that explains the project and deep-links to "Start the bot" (`t.me/<BotName>`) — not itself the delivery mechanism.
- **Admin/menu-management is the one place login *does* make sense** — but only a simple gate for the maintainer(s) (e.g. GitHub OAuth or a shared secret), never for end users/subscribers. Not built in V2 MVP; menu updates still go through the existing "re-photograph → Claude re-parses `data/menus/*.json`" workflow from V1.

## Build-order decision (2026-09-14)

- **Skipping a solo-ntfy V1 build.** Since the Telegram bot needs no login and no per-user setup beyond `/start`, the user is simply subscriber #1 of their own bot — no reason to build and later discard an ntfy-only personal path. `PLAN.md` Phase 1 is now "Telegram bot MVP," serving both the original personal-use goal and the multi-user goal at once. The ntfy-vs-Telegram-vs-WebPush comparison already written up in `ARCHITECTURE.md`/`PRD.md` still stands as the recorded reasoning for *why* Telegram won.
- **WhatsApp priced out for now.** Checked Meta's Cloud API pricing (India, 2026): Utility ~₹0.115/message, Marketing ~₹0.8631/message (7.5x), no free tier for proactive (business-initiated) messages like ours. At 100 subscribers × 4 msgs/day that's roughly ₹1,380–10,360/month depending on Meta's classification of the message (which isn't guaranteed to land in the cheap Utility bucket) — plus business verification overhead. Decision: not worth it for a free side project; revisit only as an optional paid channel if there's real demand later (`PLAN.md` Phase 5).
- **Website is a landing page only, not the delivery mechanism.** Confirmed: static, no login, no backend — see `PLAN.md` Phase 2 for scope.
- **Dropped Vercel + Upstash Redis from the design.** Telegram's Bot API supports polling (`getUpdates`), not just webhooks, so a second GitHub Actions workflow (`poll_subscribers.yml`, ~every 15 min) can capture `/start`/`/stop` without any always-on server. The subscriber list (`data/subscribers.json`) is committed to the repo, same as the menu data. Net effect: the whole project now needs exactly two accounts (GitHub, Telegram) instead of four. Trade-off accepted: new-subscriber confirmation arrives within ~15 minutes instead of instantly — fine for this use case. Full detail in `ARCHITECTURE.md` §2. (Superseded 2026-09-15: a third account, cron-job.org, was added after all — see below.)

## Privacy decision (2026-09-15)

Asked directly whether it's safe that `data/subscribers.json` sits in a public repo. Answer given: not fully — it's real, bounded exposure (only bare Telegram `chat_id`s are stored, no names/usernames/phone numbers, but the list is public and permanent in git history). Presented three options: move to a private data repo (recommended), leave it public, or make the whole repo private. **User chose: leave it public.** Recorded so a future session doesn't need to re-litigate this or assume it's an oversight — it's a considered trade-off (simplicity over that specific privacy hardening), not a bug.

## Scheduling reliability incident (2026-09-15)

**What happened:** the first real subscriber sent `/start` the evening of 2026-09-14 and didn't get a welcome reply for several hours; the next morning's breakfast notification (default 07:15 IST) never arrived at all.

**Root cause, confirmed via `gh run list` timestamps + GitHub's own documented behavior (see sources in that day's chat):** GitHub Actions' `schedule` trigger is explicitly best-effort, not guaranteed-time, and is especially prone to multi-hour delays on low-activity/public repos. Observed: only 3 scheduled runs in the first ~14 hours instead of the expected ~168 (every 5 min), including a 4+ hour gap spanning the missed breakfast. Manual `workflow_dispatch` runs during debugging, by contrast, all started within ~15 seconds — `schedule` specifically is what's throttled, not GitHub Actions generally.

**Fix, done and verified (2026-09-15):** cron-job.org (free external scheduler) now calls the `workflow_dispatch` REST API every 1 minute instead of relying on GitHub's internal `schedule` trigger (kept only as a fallback). This is the fix GitHub's own community discussions recommend for anything needing real timing precision. Setup needed a narrowly-scoped GitHub PAT (Actions: write only, this repo only) plus a cron-job.org job — both done. One setup mistake along the way: the `Authorization` header was first misconfigured with the literal word `token` in the header's *Key* field rather than `Authorization` as the key and `Bearer <PAT>` as the value — worth remembering if this ever needs reconfiguring. Confirmed working via `gh run list`: `workflow_dispatch` runs now fire roughly every 60 seconds, and a real Telegram message got a reply within about a minute (down from multi-hour delays before this fix).

**Lesson for future scheduling decisions in this project:** don't assume GitHub Actions `schedule` delivers anywhere near its configured interval, especially sub-hourly. Verify against real run timestamps (`gh run list --json createdAt`) before trusting a cron-based design, not just against the workflow YAML being syntactically valid.

## Change log

- **2026-09-14** — Project created. Docs written (`PRD.md`, `ARCHITECTURE.md`, `PLAN.md`, this file). `data/menu.json` extracted from `menu.jpeg`. Decisions locked: name = NotiMess, delivery = ntfy.sh, scheduler = GitHub Actions. Phase 1 (writing `notifier.py` + the GitHub Actions workflow) not yet started.
- **2026-09-14 (same day, later)** — GitHub repo created at `github.com/eklavyamathur9/NotiMess` (not yet connected/pushed locally). User supplied a more carefully transcribed `vit_bhopal_mess_menu_september_2026.json`, which **supersedes** the earlier `data/menu.json` extraction and corrects several medium-confidence cells that were in fact wrong — notably: Friday lunch's 3rd item is "Drumstick Sambar" (not "Lasuni Dal" as first guessed), Saturday lunch has 10 items including both "Lasuni Dal" *and* "Tomato Rice" plus "Jeera Aloo Dry" (the earlier version dropped one of these), and Sunday lunch includes "Pineapple Halwa" as the sweet (missed earlier). Moved to `data/menus/vit_bhopal_mess_menu_september_2026.json` — the `menus/` directory (plural, filename per institution+month) anticipates V2's multi-source needs. Old `data/menu.json` deleted as superseded. Scope expanded to a multi-user platform (V2) — decisions above.
- **2026-09-14 (same day, even later)** — User added the bot token as a GitHub Actions secret and made the repo public. Built FR12 (per-subscriber notification time via `/settime`, `/mytimes`, `/reset`): merged `notifier.py` + `poll_subscribers.py` into a single `tick.py` running every 5 minutes (`tick.yml`), since per-subscriber times need frequent due-checks anyway and merging removes the two-workflow subscriber-list write race. `data/subscribers.json` schema changed from a flat chat_id array to an object keyed by chat_id holding `prefs` (per-meal time) and `last_sent` (per-meal last-sent date, the dedup mechanism). No real subscribers existed yet, so no migration was needed. Unit-tested locally with mocked Telegram calls before pushing. Full design in `ARCHITECTURE.md` §11 and `PRD.md` §22.
- **2026-09-14 (same day, later still)** — Wrote and pushed the Phase 1 implementation: `notifier.py`, `poll_subscribers.py`, both GitHub Actions workflows (`notify.yml`, `poll_subscribers.yml`), `data/subscribers.json`, `requirements.txt`. Connected this folder to `github.com/eklavyamathur9/NotiMess` (`git init`, initial commit, pushed to `main`). Architecture simplified in the process — dropped the planned Vercel+Upstash webhook design for GitHub Actions polling (see decision above). Blocked on two user actions before the bot goes live: create the bot via @BotFather, and add its token as the `TELEGRAM_BOT_TOKEN` GitHub Actions secret. Repo is currently **private**; GitHub Actions minutes are unlimited on public repos vs. a 2,000 min/month cap on private ones — at ~every-15-min polling this stays well within that cap, but worth knowing if the repo is ever made public for the "let other people use it" goal.
- **2026-09-15** — User added the `TELEGRAM_BOT_TOKEN` secret and made the repo public. Debugged why the first real subscriber's welcome message was hours late and no breakfast notification arrived: traced to GitHub Actions' `schedule` trigger being unreliable at 5-min granularity (only 3 runs in ~14h instead of ~168) — see "Scheduling reliability incident" above for the full writeup and the cron-job.org fix (not yet set up — needs the user to create a PAT + cron-job.org job, `PLAN.md` Phase 1.6). Also answered and logged the public-repo privacy question ("Privacy decision" above: user chose to leave it public). Along the way, accidentally committed a deletion of `menu.jpeg` via an unreviewed `git add -A` — caught and restored from git history in the next commit; lesson: always run `git status --short` before committing after a broad `add`, even mid-debugging.
- **2026-09-15 (same day, later)** — User clarified `menu.jpeg`'s removal was intentional (not the earlier accident) — re-deleted per their actual wish; no longer referenced in `ARCHITECTURE.md`'s directory tree. Built FR13: on-demand `/menu` command that returns whichever meal is currently open, the next one opening today, or tomorrow's breakfast if today's are all done — works without `/start`, and the bot's command list is registered with Telegram (`setMyCommands`) so it's tappable from the native "/" picker, matching the "an option to click" ask rather than a typed command. Unit-tested locally across five time-of-day cases before pushing. Answered the website status question: not started, and recommended sequencing it *after* Phase 1.6 (reliable scheduling) rather than in parallel — pushing people toward a bot that isn't reliably notifying yet would cost more trust than a landing page adds; a bare `t.me/` link is enough to onboard people via word of mouth in the meantime.
- **2026-09-15 (same day, even later)** — Walked the user through the cron-job.org Advanced tab field-by-field (they'd put `token` in the header's Key field instead of `Authorization`) and confirmed the fix live via `gh run list`: `workflow_dispatch` runs now fire every ~60 seconds, and a real message got a reply within about a minute. Phase 1.6 and Phase 1.7 both closed out as done. See "Bot feature ideas" below for what was suggested next.
- **2026-09-15 (same day, still later)** — Built FR14 (`/full`, all four meals in one message) and FR15 (`/vegonly`, per-subscriber non-veg filtering) from the feature-idea list. `strip_nonveg()` handles the menu data's inconsistent veg/non-veg ordering (one item lists non-veg first, the other five list veg first) — tested against all 6 combined items in the dataset before pushing. `data/subscribers.json` schema grew a `veg_only` boolean per subscriber, default false. Full design in `ARCHITECTURE.md` §13, `PRD.md` §24–25.
- **2026-09-21** — User supplied `vit_bhopal_mess_menu_week2_september_2026.json` and asked to swap it in for the active (Week 1) menu. **Confirms the "does the mess rotate weekly" open question from `PRD.md` §11 — it does:** Week 2 went effective exactly 7 days after Week 1 (14→21 Sept). Not yet confirmed: whether it's a clean 2-week loop back to Week 1 on 28 Sept, or continues further — a decision on whether to automate rotation was deliberately deferred to `PLAN.md` Phase 4 rather than built from two data points. Two real issues caught and fixed before shipping, not just a data swap: (1) the raw file's shape didn't match the canonical schema (flat `breakfast`/`lunch`/... keys + `days` sub-key, vs. the established `meals` wrapper + `items` sub-key) — converted before use, same pattern any future user-supplied file should get, not just photo extractions; (2) `strip_nonveg()` assumed (from Week 1's data alone) that the kept veg half of a combined item always carries an explicit `"(Veg)"` tag — Week 2 tags *only* the non-veg half (e.g. `"Veg Kolapuri / Kolhapuri (Non-Veg)"`, no tag on the veg side at all), which would have made `/vegonly` silently stop filtering on this data. Found by enumerating every `(Non-Veg)`-tagged item in the new file *before* swapping, not after. Fixed by keeping whichever half lacks `"(Non-Veg)"` regardless of whether the other half is tagged, re-verified against every combined item in both weeks plus plain veg items (no regressions). `effective_from` set to 2026-09-21 (the actual swap date) rather than the poster's nominal "14th September" text, since that date is shared by both weeks' posters and can't disambiguate which is active. Verified live afterward via `gh run list` and run logs — no errors, correct data loading.

## Bot feature ideas (suggested 2026-09-15)

Brainstormed when asked "what else could the bot do." Two were picked and built same-day (below); the rest are still just ideas — nothing here should be assumed built until `PLAN.md` says so.

**Built:**
- **Veg-only filtering (`/vegonly`)** and **`/full` for the whole day** — see `PRD.md` §24–25, `ARCHITECTURE.md` §13.

**Still just ideas, roughly ordered by effort:**
- **Mute for N days** — `/mute 3` to skip notifications while traveling/home, without unsubscribing entirely. Needs a `muted_until` date field per subscriber, checked alongside `last_sent` in `send_due_notifications`.
- **Weekly digest** — one message Sunday night with the whole coming week's menu, for people who'd rather read it once than get pinged 28 times. Reuses `build_message` in a loop, new weekly cron-job.org job.
- **Group chat support** — add the bot to a hostel wing's group chat instead of (or alongside) DMs; needs privacy-mode handling and a `chat_id` that's negative (group IDs are negative in Telegram) — `send_due_notifications` and `handle_command` are chat_id-type-agnostic already, so this is mostly testing, not rewriting.
- **Ratings/feedback loop** — thumbs up/down on a meal, aggregated per dish over time; meaningfully bigger (needs its own storage shape, an inline keyboard for the reaction, and a decision on whether feedback is public or private).
- **Admin broadcast** — a maintainer-only `/broadcast <text>` to reach all subscribers for announcements (e.g. "menu updated for October"), gated by checking the sender's chat_id against a hardcoded owner ID.
